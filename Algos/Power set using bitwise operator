Power set of a string of length n will contain pow(2,n) elements.

Basically we'll use the binary representations from [1, length of string] to decide which character of the string has to be present in a given set
